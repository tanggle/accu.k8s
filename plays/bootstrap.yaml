---
# Playbook for AccuInsight+ Bootstrap

- name: AccuInsight+ OS Default Settings
  hosts: kube-cluster,accu-server
  gather_facts: true
  become: yes 
  any_errors_fatal: "{{ any_errors_fatal | default(true) }}"
  roles:
    - { role: bootstrap }


