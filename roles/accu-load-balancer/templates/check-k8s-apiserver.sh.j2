#!/bin/sh

Exit() {
    echo "*** $*" 1>&2
    exit 1
}

curl --silent --max-time 2 --insecure https://localhost:{{ ext_lb_port }}/ -o /dev/null || Exit "K8S API Server is UNHEALTY on '{{ inventory_hostname }}'"
